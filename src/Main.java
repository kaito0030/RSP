import Controll.GameController;

public class Main {
    public static void main(String[] args) {
        new GameController();
    	
    		//画面確認用
    	/*
    		MainFrame frame=new MainFrame();
    		TitlePanel title=new TitlePanel();
    		RoundPanel round=new RoundPanel();
    		SelectPanel select=new SelectPanel();
    		ResultPanel result=new ResultPanel();
    		ContinuePanel cont=new ContinuePanel();
    		FinalPanel fin=new FinalPanel();
    		
        frame.addScreen(title,"TITLE");
        frame.addScreen(round,"ROUND");
        frame.addScreen(select,"SELECT");
        frame.addScreen(result,"RESULT");
        frame.addScreen(cont,"CONTINUE");
        frame.addScreen(fin,"FINAL");
        frame.setVisible(true);
        round.update(50,10,10);
        result.setResultText("勝ち");
        result.setScore(10, 10);
        result.setHands(Hand.GU, Hand.CHOKI);
        result.setName("皆斗");
        fin.setFinal(23,30);
        frame.showScreen("FINAL");
        */
    }
}